.royalSlider.rsDefault.image-slider
  =image_tag("ui-hr-v1.0.png", :class => "rsImg", :alt => "hr v1", :data => { :description => "I want to lay out the pages representing your services as editorial features. It would give them their own identity within the site and the familiar format is ideal for introducing these services in an unexpected way that gives the combined benefit of not requiring the user to figure out the interface and not boring the user with it.</br></br>In this round of design I see only subtle differences amongst the various service layouts. Down the road they can be given more individual design attention if that is so desired."})
  =image_tag("spacecat.png", :class => "rsImg", :alt => "space cat", :data => { :description => "contemplative ginger space cat"})
